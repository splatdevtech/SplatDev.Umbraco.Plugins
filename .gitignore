# ============================================================
# Visual Studio
# ============================================================

.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.rsuser

# Build results
bin/
obj/
[Bb]uild[Ll]og.*
[Tt]est[Rr]esult*/
TestResult.xml
BenchmarkDotNet.Artifacts/

# Publish
[Pp]ublish/
*.pubxml
!*.pubxml.user
*.azurePubxml
*.publishsettings
PublishScripts/

# NuGet
*.nupkg
*.snupkg
[Pp]ackages/
!**/[Pp]ackages/build/
*.nuget.props
*.nuget.targets
project.lock.json
project.fragment.lock.json

# Misc
*.cache
# git has core.ignorecase=true here, so the rule above also matches the
# SplatDev.Cache project directory and silently ignores anything new added to it
# (its icon.png needed -f). Keep the project tracked.
!/SplatDev.Cache/
!/SplatDev.Cache/**
# ...but not its build output. The two lines above re-include everything under that
# project, which defeated the bin/ and obj/ rules above and let `git add -A` commit 77
# build artefacts.
/SplatDev.Cache/**/bin/
/SplatDev.Cache/**/obj/
*.log
*.tmp
*.tmp_proj
*_wpftmp.csproj
*.tlh
*.tli
*.vspscc
*.vssscc
.builds
*.pidb
*.svclog
*.scc
*.aps
*.ncb
*.opendb
*.opensdf
*.sdf
*.VC.db
*.VC.VC.opendb
ipch/
artifacts/

# ============================================================
# Umbraco
# ============================================================

# Runtime / generated data
**/App_Data/
**/umbraco/Data/TEMP/
**/umbraco/Logs/
**/umbraco/medialibrary/
**/umbraco/models/
**/umbraco/cache/

# Media uploads
**/wwwroot/media/

# Umbraco backoffice (bundled at runtime for v9+)
**/wwwroot/umbraco/

# ============================================================
# Node / npm
# ============================================================

node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ============================================================
# Local secrets / credentials
# ============================================================

Credentials.md

# ============================================================
# VS Code
# ============================================================

.vscode/
**/.vscode/

# ============================================================
# .bin folders
# ============================================================

.bin/
**/.bin/

# WhatsApp plugin: local client build artefacts
SplatDev.Umbraco.Plugins.WhatsApp/client/node_modules/

# Playwright MCP session artifacts (screenshots, snapshots, console logs)
.playwright-mcp/

# Playwright screenshots captured into the repo root
wa-*.png

# node_modules is symlinked between plugin clients by build-all-plugins.sh to save
# disk. A trailing-slash pattern does not match a symlink, so 59 of them were tracked as
# links into an absolute path on one developer machine.
**/client/node_modules

# The Umbraco 17 baseline is a local test site: its database, generated Views, config and
# umbraco/ runtime folder are per-machine state, not source. `git add -A` swept its live
# SQLite files into a commit once; these keep that from repeating.
test-environments/*/umbraco/
test-environments/*/Views/
test-environments/*/config/
test-environments/**/*.sqlite.db
test-environments/**/*.db-shm
test-environments/**/*.db-wal

# Python bytecode. tools/*.py are run directly and by CI; importing one for a test
# leaves a __pycache__ beside it, and a plain `git add -A` then commits it. Two .pyc
# files reached master that way, one of them in the commit that fixed the unlist
# planner.
__pycache__/
*.py[cod]
# ─── Agent workspace artefacts ────────────────────────────────────────────
# An agent running `git add -A` inside its workspace swept all of this into
# splatdevtech/derma-inova-web PR #48 on 2026-09-14: a dump of the container
# environment holding 11 live secrets (including an encryption key and a
# base64 SSH private key), four Terraform release archives, a bare terraform
# binary, and 50 agent skill files — +9067 lines. None of it reached a default
# branch, but the objects stayed reachable through the open PR for ten days.
# These rules exist so the accident cannot repeat, in any repo an agent works in.
environment_variables.txt
environment_variables*.txt
/terraform
/terraform.rc
terraform_*_linux_amd64.zip
.terraform/
*.tfstate
*.tfstate.*
.paperclip/