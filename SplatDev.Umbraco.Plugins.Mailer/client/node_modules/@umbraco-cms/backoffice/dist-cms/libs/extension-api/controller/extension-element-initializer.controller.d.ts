import type { UmbExtensionRegistry } from '../registry/extension.registry.js';
import type { ManifestCondition, ManifestElement, ManifestWithDynamicConditions } from '../types/index.js';
import { UmbBaseExtensionInitializer } from './base-extension-initializer.controller.js';
import type { UmbControllerHost } from '../../controller-api/index.js';
/**
 * This Controller manages a single Extension and its Element.
 * When the extension is permitted to be used, its Element will be instantiated and available for the consumer.
 * @example
 * ```ts
 * const controller = new UmbExtensionElementController(host, extensionRegistry, alias, (permitted, ctrl) => { console.log("Extension is permitted and this is the element: ", ctrl.component) }));
 * ```
 * @class UmbExtensionElementController
 */
export declare class UmbExtensionElementInitializer<ManifestType extends ManifestWithDynamicConditions = ManifestWithDynamicConditions, ControllerType extends UmbExtensionElementInitializer<ManifestType, any> = any, ExtensionInterface extends ManifestElement = ManifestType extends ManifestElement ? ManifestType : never, ExtensionElementInterface extends HTMLElement | undefined = ExtensionInterface['ELEMENT_TYPE']> extends UmbBaseExtensionInitializer<ManifestType, ControllerType> {
    #private;
    /**
     * The component that is created for this extension.
     * @readonly
     * @type {(HTMLElement | undefined)}
     */
    get component(): ExtensionElementInterface | undefined;
    get properties(): Record<string, unknown> | undefined;
    set properties(newVal: Record<string, unknown> | undefined);
    constructor(host: UmbControllerHost, extensionRegistry: UmbExtensionRegistry<ManifestCondition>, alias: string, onPermissionChanged: (isPermitted: boolean, controller: ControllerType) => void, defaultElement?: string);
    protected _conditionsAreGood(): Promise<boolean>;
    protected _conditionsAreBad(): Promise<void>;
    destroy(): void;
}
