export declare const clamp: (value: number, min: number, max: number) => number;
/**
 * Reverses a number within a range
 *
 * @param {number} num - number to reverse
 * @param {number} min
 * @param {number} max
 * @return {*}  {number}
 * @example
 * // returns 8
 * reverseNumberInRange (2, 0, 10)
 */
export declare const reverseNumberInRange: (num: number, min: number, max: number) => number;
export declare const toHex: (value: number) => string;
