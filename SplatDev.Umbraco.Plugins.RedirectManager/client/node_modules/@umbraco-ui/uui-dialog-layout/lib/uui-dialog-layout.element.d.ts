import { LitElement } from 'lit';
/**
 * @element uui-dialog-layout
 * @slot - Use this for the text content
 * @slot headline - Use this for slotted headline
 * @slot actions - Use this for actions
 * @description - Default dialog layout
 */
export declare class UUIDialogLayoutElement extends LitElement {
    /**
     * Headline for this notification, can also be set via the 'headline' slot.
     * @type string
     * @attr
     * @default null
     */
    headline: string | null;
    private _headlineSlotHasContent;
    private _headlineSlotChanged;
    /**
     * Renders a h3 with the headline slot nested
     * @returns {TemplateResult}
     * @protected
     * @method
     */
    protected renderHeadline(): import("lit").TemplateResult<1>;
    /**
     * Renders default slot
     * @returns {TemplateResult}
     * @protected
     * @method
     */
    protected renderContent(): import("lit").TemplateResult<1>;
    /**
     * Renders actions slot
     * @returns {TemplateResult}
     * @protected
     * @method
     */
    protected renderActions(): import("lit").TemplateResult<1>;
    render(): import("lit").TemplateResult<1>;
    static styles: import("lit").CSSResult[];
}
